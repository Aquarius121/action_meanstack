
$(function() {
    // Page uses stuff that is generated from server-scoped values, so look at product.jade as well

    $( ".ui-tooltip" ).tooltip({});
    //star_rating_handler.init($('.star-container'), 'self-help', '');
});
